<h1>Groups:</h1>
<% @groups.each do |group| %>
  <div class="roundbox text-center">
    <h1><%= link_to group.name, group_path(group) %></h1>
  </div>
<% end %>
<button class="btn btn-primary">
<%= link_to "Add a New Group", new_group_path, class: "text-light" %>
</button>
